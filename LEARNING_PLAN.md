# GameStore 학습 계획

## 학습 원칙

- 전체 프로젝트를 한 번에 구현하지 않는다.
- 한 수업은 설명, 짧은 직접 작성 연습, 실행 또는 테스트, 이해 확인 순서로 진행한다.
- 오류가 발생하면 수정 전에 오류 메시지, 재현 조건, 발생 계층과 원인 가설부터 확인한다.
- IntelliJ IDEA와 Windows PowerShell 환경을 기준으로 진행한다.
- 첫 번째 완성본은 게임 목록, 게임 상세, 검색으로 제한한다.
- 장바구니, 회원, 관리자 권한과 주문은 첫 완성 이후에 구현한다.

## 예정 폴더 구조

프로젝트 파일은 실제 수업에서 필요한 시점에 하나씩 만든다.

```text
GameStore/
├─ frontend/                 React 프로젝트
│  └─ src/
│     ├─ assets/             이미지와 정적 파일
│     ├─ components/         재사용 UI
│     ├─ pages/              페이지 단위 화면
│     ├─ layouts/            공통 배치
│     ├─ data/               초기 가짜 데이터
│     ├─ services/           백엔드 API 호출
│     ├─ hooks/              재사용 React 로직
│     ├─ context/            필요해진 뒤 도입할 전역 상태
│     └─ utils/              보조 함수
├─ backend/                  Spring Boot 프로젝트
│  └─ src/
│     ├─ main/
│     │  ├─ java/.../gamestore/
│     │  │  ├─ controller/
│     │  │  ├─ service/
│     │  │  ├─ repository/
│     │  │  ├─ entity/
│     │  │  ├─ dto/
│     │  │  ├─ security/
│     │  │  ├─ exception/
│     │  │  └─ config/
│     │  └─ resources/
│     └─ test/
├─ docs/
└─ README.md
```

# 첫 번째 완성본

완성 기준은 React 화면이 Spring Boot와 DB에서 데이터를 받아 게임 목록, 상세, 이름 검색을 제공하는 것이다. 장바구니, 로그인, 관리자와 주문은 포함하지 않는다.

## 0단계: 개발 흐름과 도구

### 0-1. 전체 데이터 흐름

- 브라우저, React, Spring Boot, DB의 역할
- HTTP 요청과 응답
- JSON의 역할
- 게임 목록 요청이 DB까지 이동하고 돌아오는 과정

확인: 전체 요청 흐름을 본인 말로 설명한다.

### 0-2. Windows 개발 도구

- IntelliJ IDEA
- Windows PowerShell
- Node.js와 npm
- Java와 JDK
- Git

확인: 각 도구가 담당하는 역할을 구분한다.

## 1단계: JavaScript 필수 기초

### 1-1. 변수와 기본 자료형

- `const`, `let`
- 문자열, 숫자, 불리언
- `null`, `undefined`
- 비교, 조건문, 템플릿 문자열

연습: 게임 이름, 가격과 할인 여부를 변수로 표현한다.

### 1-2. 함수

- 함수 선언과 호출
- 매개변수와 반환값
- 화살표 함수
- 콜백 함수 기초

연습: 가격과 할인율을 받아 할인가를 반환한다.

### 1-3. 배열과 객체

- 배열과 객체
- 배열 안의 객체
- 직접 변경과 복사
- 불변성 기초

연습: 게임 객체 세 개를 배열로 관리한다.

### 1-4. `map`, `filter`, `find`

- `map`으로 변환
- `filter`로 선택
- `find`로 한 항목 찾기

연습: 게임 이름 목록, 가격 조건 검색과 ID 검색을 작성한다.

### 1-5. 구조 분해와 전개 문법

- 객체와 배열 구조 분해
- 나머지 문법
- 전개 문법
- 객체와 배열 복사

연습: 게임 객체를 복사하면서 가격만 변경한다.

### 1-6. 모듈

- 파일 분리 기준
- `export`, `import`
- 기본 내보내기와 이름 있는 내보내기

연습: 게임 데이터와 가격 함수를 별도 모듈로 분리한다.

### 1-7. Promise

- 동기와 비동기
- Promise의 대기, 성공, 실패
- `.then()`, `.catch()`

연습: 지연 후 게임 데이터를 반환하고 성공과 실패를 처리한다.

### 1-8. `async/await`

- `async`, `await`
- `try/catch`
- 로딩, 성공, 실패 상태

연습: Promise 로직을 `async/await`로 바꾼다.

단계 확인: React 없이 간단한 게임 검색을 작성하고 비동기 흐름을 설명한다.

## 2단계: React 정적 화면

### 2-1. React와 JSX

- React 프로젝트 구조
- 컴포넌트와 JSX
- `main`과 `App`
- 개발 서버 실행

확인: PowerShell에서 실행하고 브라우저와 콘솔 오류를 확인한다.

### 2-2. 게임 카드와 props

- 부모와 자식 컴포넌트
- props와 구조 분해
- 재사용 가능한 UI

연습: 서로 다른 게임 카드 두 개를 props로 표시한다.

### 2-3. 게임 목록

- 배열과 `map`
- React의 `key`
- 데이터와 화면 분리

확인: 카드 수와 데이터 수가 일치하고 콘솔 경고가 없는지 확인한다.

### 2-4. CSS와 레이아웃

- Header와 카드 그리드
- Flexbox와 Grid
- 기본 반응형 배치

확인: 화면 폭, 긴 제목과 누락 이미지 상황을 수동 확인한다.

## 3단계: React 상태와 검색

### 3-1. 이벤트와 `useState`

- 이벤트 처리
- 일반 변수와 상태의 차이
- 입력창과 상태 연결

연습: 입력 검색어를 화면에 표시한다.

### 3-2. 게임 검색

- 검색 상태와 `filter`
- 대소문자 처리
- 원본 목록과 파생 결과

확인: 일치, 불일치, 빈 검색어와 대소문자 차이를 확인한다.

### 3-3. 결과 없음 처리

- 조건부 렌더링
- 빈 배열 처리

연습: 검색 결과가 없을 때 안내 문구를 표시한다.

## 4단계: React 페이지와 게임 상세

### 4-1. React Router

- URL과 화면
- 라우팅과 링크
- 페이지 컴포넌트

확인: 링크 이동과 주소 직접 입력을 확인한다.

### 4-2. 게임 상세

- URL 매개변수
- `find`
- 존재하지 않는 게임 처리

확인: 여러 정상 ID와 없는 ID를 확인한다.

### 4-3. 프론트엔드 중간 정리

- 컴포넌트 책임과 중복 확인
- 콘솔 오류와 경고 제거
- 목록, 상세, 검색 회귀 확인
- 검색 순수 함수에 작은 테스트 작성

## 5단계: Java 기초

### 5-1. 실행 구조와 기본 문법

- JDK와 JVM
- 클래스와 `main`
- 변수, 조건문, 반복문, 메서드

연습: 할인 가격을 계산한다.

### 5-2. 클래스와 객체

- 필드, 생성자와 메서드
- 접근 제어자와 캡슐화

연습: 게임 ID, 이름과 가격을 가진 객체를 만든다.

### 5-3. 컬렉션

- `List`, `Map`, 제네릭
- Stream의 기본 `map`, `filter`

연습: 가격 조건에 맞는 게임을 찾는다.

### 5-4. 인터페이스와 예외

- 인터페이스와 구현체
- 예외와 `try/catch`
- 오류 전달

확인: 정상과 예외 흐름을 실행하고 가격 또는 검색 로직에 작은 JUnit 테스트를 작성한다.

## 6단계: Spring Boot 기초

### 6-1. 프로젝트 구조

- Spring Boot의 역할
- 시작 클래스, 의존성, 설정 파일과 서버 포트

확인: IntelliJ와 PowerShell에서 서버를 실행한다.

### 6-2. Controller와 HTTP

- HTTP 메서드와 상태 코드
- URL, JSON, 요청과 응답
- Controller

연습: 고정된 게임 하나를 JSON으로 반환한다.

### 6-3. Service와 의존성 주입

- Controller와 업무 로직 분리
- Service와 생성자 주입

확인: Service에 작은 단위 테스트를 작성한다.

### 6-4. 메모리 Repository

- Repository의 책임
- 전체 조회와 ID 조회
- 없는 데이터 처리

확인: 전체, 단건과 없는 ID 흐름을 확인한다.

## 7단계: 데이터베이스와 조회 API

### 7-1. 관계형 DB 기초

- 테이블, 행, 열과 기본키
- SQL `SELECT`
- H2와 MySQL의 차이

### 7-2. JPA Entity와 Repository

- Entity, JPA와 ORM
- Spring Data JPA

확인: 저장 후 DB에서 다시 조회한다.

### 7-3. DTO 분리

- Entity 직접 노출의 문제
- 요청 및 응답 DTO
- 필요한 데이터만 응답하기

### 7-4. 목록과 상세 API

- 게임 전체 조회
- ID 상세 조회
- 없는 게임 오류 응답

확인: Service 테스트와 API 수동 확인을 병행한다.

### 7-5. 검색 API

- 쿼리 파라미터
- 부분 검색과 대소문자 기준
- 빈 검색어 처리

확인: 전체 이름, 일부 이름, 결과 없음과 빈 검색어를 테스트한다.

## 8단계: React와 Spring Boot 연결

### 8-1. API 호출

- `fetch` 또는 Axios
- `Promise`, `async/await`
- `useEffect`
- JSON과 CORS

확인: Network 탭에서 실제 요청과 응답을 확인한다.

### 8-2. 로딩과 오류 상태

- 로딩, 성공, 실패와 빈 데이터

확인: 정상, 느린 응답, 서버 중지와 빈 목록을 확인한다.

### 8-3. 상세 API 연결

- URL ID로 상세 요청
- 없는 게임과 서버 오류 구분

확인: 정상 ID, 없는 ID, 잘못된 ID와 서버 중지를 확인한다.

### 8-4. 검색 API 연결

- 검색어 쿼리 파라미터
- URL 인코딩
- 검색 버튼 방식으로 시작

확인: 한글 및 영문, 공백, 특수문자, 결과 없음과 연속 검색을 확인한다.

## 9단계: 첫 번째 완성본 검증

- 게임 목록, 상세와 이름 검색 동작
- 없는 게임, 빈 결과와 서버 오류 구분
- 프론트엔드 콘솔 오류 없음
- 처리되지 않은 백엔드 예외 없음
- Controller, Service와 Repository 책임 분리
- 핵심 Service/API 테스트 통과
- README에 실행 순서 기록

종료 조건: React부터 DB까지의 요청 흐름, 각 백엔드 계층, Entity와 DTO, 동기와 비동기를 설명할 수 있어야 한다.

# 후속 확장

## 10단계: 인증 없는 일반 게임 CRUD

### 10-1. 등록

- POST, 요청 DTO, 입력 검증과 `201 Created`
- Service 테스트, API 요청과 DB 저장 확인

### 10-2. 수정

- PUT과 PATCH
- 대상 조회와 변경 필드
- 정상, 없는 ID와 잘못된 가격 확인

### 10-3. 삭제

- DELETE
- 실제 삭제와 논리 삭제의 차이
- 삭제 전후 DB와 API 확인

### 10-4. React 관리 화면

- 등록, 수정 폼과 삭제 버튼
- 입력 오류 표시

이 단계에서는 CRUD를 먼저 배우며 관리자 권한은 붙이지 않는다.

## 11단계: 장바구니

### 11-1. props와 `useState`

- 부모가 상태 소유
- props 전달과 자식 이벤트
- 추가, 중복, 삭제, 빈 장바구니와 총액 확인

### 11-2. 불편함 확인

- 여러 단계에 걸친 props 전달
- 사용하지 않는 중간 컴포넌트의 전달 역할
- 여러 화면에서 상태가 필요한 문제

### 11-3. Context 도입

- Provider와 `useContext`
- 전역 상태가 필요한 기준
- 불필요한 props가 실제로 줄었는지 비교
- 장바구니 로직 작은 테스트

### 11-4. `localStorage`

- JSON 저장과 복원
- 새로고침 유지
- 손상된 저장 데이터 처리

## 12단계: 회원과 인증 개념

### 12-1. 회원 데이터

- 회원 Entity
- 이메일 중복과 입력 검증
- 비밀번호 해시
- DB에 평문 비밀번호가 없는지 확인

### 12-2. 세션과 JWT 비교

- 세션은 인증 상태를 서버에 저장한다.
- JWT는 서명된 토큰을 클라이언트가 보관한다.
- 세션은 로그아웃과 강제 만료가 비교적 단순하다.
- JWT는 분산 구조에 유리하지만 갱신, 폐기와 안전한 저장이 복잡하다.

이 프로젝트는 단일 Spring Boot 서버이므로 **서버 세션과 HttpOnly 쿠키**를 사용한다. 서버 통제가 쉽고 초보자가 JWT를 `localStorage`에 보관하는 위험한 구현을 피할 수 있기 때문이다. 세션에서도 HttpOnly, Secure, SameSite, CORS와 CSRF를 함께 학습한다.

## 13단계: 로그인과 권한

### 13-1. Spring Security

- 인증과 인가
- Security Filter Chain
- 로그인, 로그아웃과 세션 만료
- 정상, 잘못된 비밀번호와 없는 사용자 확인

### 13-2. React 로그인 상태

- 로그인 폼과 현재 사용자 조회
- 새로고침 후 상태 복원
- 쿠키 포함 요청과 인증 오류

### 13-3. 관리자 권한

- 일반 사용자와 관리자 역할
- 백엔드 API 권한 검사
- `401`과 `403`
- 비로그인 및 일반 사용자 차단, 관리자 허용 확인

관리자 권한은 일반 CRUD가 완성된 이 시점에 추가한다.

## 14단계: 서버 장바구니

- 사용자별 장바구니와 항목
- 프론트엔드와 서버 장바구니 동기화
- 비로그인 장바구니와 로그인 후 병합 정책
- 회원별 격리, 중복, 삭제와 권한 테스트

## 15단계: 가상 주문

### 15-1. 주문 데이터

- 주문과 주문 항목
- 구매 시점 가격 저장
- 과거 주문 가격 불변 테스트

### 15-2. 주문 생성

- 서버 가격 재계산
- 트랜잭션
- 성공 후 장바구니 비우기
- 정상, 빈 장바구니, 조작 가격과 중간 실패 테스트

### 15-3. 구매 내역

- 본인 주문 목록과 상세
- 다른 회원 주문 접근 차단

## 16단계: 선택 확장

- 위시리스트
- 리뷰와 평점
- 페이지네이션
- 정렬과 장르 필터
- 관리자 대시보드
- 이미지 업로드
- MySQL 전환
- Docker와 배포
- JWT 비교 구현

확장은 한 번에 하나만 선택하고 요구사항 정의, 실패 사례, 핵심 테스트, API 수동 확인, React 연결과 전체 회귀 확인 순서로 진행한다.

## 다음 시작점

2026-09-09 기준으로 0단계부터 4단계까지 완료했다. 4단계에서는 React Router, 게임 상세 조회, 검색 함수 분리와 자동 테스트 5개를 학습했다. 코드 리뷰를 마쳤고, 사용자가 IntelliJ 터미널에서 테스트·lint·build 통과와 브라우저 동작 및 Console 오류·경고 없음을 확인했다.

다음은 **5-1. Java 실행 구조와 기본 문법**이다. 현재 파일과 개발 환경을 읽기 전용으로 확인한 뒤 JDK와 JVM, 클래스와 `main`부터 설명한다. 설명 → 사용자가 IntelliJ에서 직접 작성 → IntelliJ 터미널에서 직접 실행 → 코드와 실행 결과 리뷰 순서를 유지한다. 기존 변경 파일과 미추적 파일은 임의로 수정하거나 커밋하지 않는다.
